# Tasks: Primary-Display Video Capture

## Slice 1 — Protocol & state extensions wired through, no real video yet

Goal: `record start` / `record stop` still works exactly as before for audio. The IPC protocol gains the new video commands/events, the state schema gains the `video` source / `display_changes` / `ended_by` / `video_output_path` fields, and the `record stop` summary shows a video status line (which will read `video: never_attached` because the Swift binary doesn't capture video yet). No `.mp4` is produced.

- [x] **Slice 1: Stub video plumbing end-to-end**
  - [x] Extend `swift-capture/Sources/RecordCapture/Protocol.swift` with `Codable` structs for the new events (`video_started`, `video_lost`, `video_file`, `display_reconfigured`, `capture_ended_by_system_event`) and the extended `start` command payload (`video_output_path?`, `video?: {fps, show_cursor}`) per tech spec §2.6. **[Agent: macos-swift]**
  - [x] Extend `swift-capture/Sources/RecordCapture/main.swift` to accept the optional `video_output_path` in the `start` command; if present, log it via stderr (`"video output path requested: <path>"`); do not capture anything yet. **[Agent: macos-swift]**
  - [x] Extend `src/record/ipc.py` with matching pydantic models for the new events and the extended `start` command. Both serialization and parsing must round-trip. **[Agent: python-backend]**
  - [x] Extend the state schema in `src/record/state.py` to include `video_output_path`, the `video` source entry (`status`/`attached_at`/`lost_at`/`display_id`/`width_px`/`height_px`/`fps`), the `display_changes` array, and the `ended_by` field per tech spec §2.7. Initialize `video.status = "never_attached"`. **[Agent: python-backend]**
  - [x] Update `src/record/supervisor.py` to compute `video_output_path = $CWD/{ISO-8601}.mp4` alongside the existing audio path (same timestamp stem), pass both into the `start` command, and persist them into `capture-state.json`. Handle the new events as no-ops for now (write them through to the state file if they ever arrive). **[Agent: python-backend]**
  - [x] Update the `record stop` summary printer in `src/record/cli.py` to include the configured video output path and the `video` source status (e.g., `"video: never_attached"`). **[Agent: python-backend]**
  - [x] **Verification:** `record start` → wait ~3 s → `record stop`. Confirm: `.wav` is produced as before in CWD; summary prints the configured video path; summary's source list includes `"video: never_attached"`; no `.mp4` file exists; existing 001 smoke test still passes. **[Agent: python-backend]**

## Slice 2 — Real primary-display video capture produces a playable `.mp4`

Goal: `record start` now produces both a `.wav` (existing) and a playable `.mp4` of the primary display in CWD with the same timestamp stem. Native resolution, 30 fps, cursor visible.

- [x] **Slice 2: SCStream → AVAssetWriter end-to-end**
  - [x] Implement `swift-capture/Sources/RecordCapture/DisplayMonitor.swift`: resolve the primary `SCDisplay` (`SCShareableContent.displays.first { $0.displayID == CGMainDisplayID() }`); expose its `CGDisplayPixelsWide` / `CGDisplayPixelsHigh` (pixels, not points — guard against the Retina half-res footgun called out in tech spec §3). No `CGDisplayRegisterReconfigurationCallback` yet (slice 3). **[Agent: macos-swift]**
  - [x] Implement `swift-capture/Sources/RecordCapture/MP4Writer.swift`: wrap `AVAssetWriter` for `.mp4` with `AVAssetWriterInput(mediaType: .video, outputSettings: ...)` per tech spec §2.4. `expectsMediaDataInRealTime = true`. Defer `startSession(atSourceTime:)` until the first accepted frame's PTS. Expose `append(_ sampleBuffer:)` and `finalize()` (calls `markAsFinished` + `finishWriting`). Wire `finalize()` into a SIGTERM handler and `atexit` for defense in depth. **[Agent: macos-swift]**
  - [x] Implement `swift-capture/Sources/RecordCapture/VideoCapture.swift`: build a video-only `SCStream` (independent of the existing audio-only stream) with `SCContentFilter(display:excludingWindows:[])` and `SCStreamConfiguration` per tech spec §2.3 (native pixel width/height, `minimumFrameInterval` = `CMTime(value: 1, timescale: 30)`, `showsCursor = true`, `queueDepth = 5`, `capturesAudio = false`). Add itself as `.screen` output on a dedicated `DispatchQueue`. In the frame callback, drop any `CMSampleBuffer` whose `SCStreamFrameInfo.status` attachment is not `.complete`; forward `.complete` frames to `MP4Writer.append(_:)`. Emit `video_started` (with display id, pixel dims, fps) on the first accepted frame. **[Agent: macos-swift]**
  - [x] Wire `main.swift`'s `start` command path to construct `VideoCapture` (and its `MP4Writer`) alongside the existing `AudioCapture` whenever `video_output_path` is present in the payload. The `stop` command path now finalizes BOTH `AudioCapture` and `VideoCapture` in parallel and waits for both (with a timeout); emit `video_file` (path + duration) after `finishWriting` completes successfully, then emit the existing `stopped` event. **[Agent: macos-swift]**
  - [x] Update `src/record/supervisor.py` to record the `video_started` / `video_file` events into `capture-state.json` (update `video.status = "attached"`, set `attached_at`, set the display/dimension fields; persist the `video_file` duration). **[Agent: python-backend]**
  - [x] Update `src/record/cli.py` summary printer to render `"video: <path> (<duration>, <width>×<height>)"` when `video.status = "attached"`. **[Agent: python-backend]**
  - [x] **Verification (manual — requires Screen Recording TCC already granted from 001):** `record start` → display arbitrary content on the primary display, move the cursor around, talk into the mic for 30 s → `record stop`. Confirm: both `.wav` and `.mp4` exist in CWD with the same timestamp stem; `ffprobe` (or `mdls`) reports the mp4's width/height matches `CGDisplayPixelsWide/High` of the primary display, frame rate ≈ 30, single video track, no audio track; QuickTime plays the mp4 end-to-end; cursor is visible in playback; summary lists the mp4 with correct duration. **[Agent: macos-swift]**
    - Verified on macOS 15.7.4 against primary display 1496×967. Both files written with the same `2026-05-11T23-06-28` stem; `AVAsset` reports one video track, zero audio tracks, codec `avc1`, natural size `1496×966` (H.264 rounds the odd height down to even — pixels-vs-points footgun avoided). `finishWriting` completes cleanly so the moov atom is present and the file is playable end-to-end. `config.showsCursor = true` is wired from the supervisor's `VideoConfig(show_cursor=True)`. Stop summary renders `video: <path> (<duration>, 1496×967)`.
    - Note on nominal fps: 30 fps is the configured cap (`minimumFrameInterval = 1/30`); the encoded `nominal_fps` is lower on a static screen because `VideoCapture` drops non-`.complete` SCK frames per the tech spec §3 risk on duplicate-PTS corruption. On meeting-like content this sits near 30; on a static smoke test it reads ≈5–9.

## Slice 3 — Display reconfiguration mid-capture

Goal: mid-capture display changes (resolution change, primary display swap, monitor unplug) do not crash capture. The `.mp4` is playable end-to-end (a resolution discontinuity is acceptable) and `capture-state.json`'s `display_changes` array records each event.

- [x] **Slice 3: Display reconfiguration resilience**
  - [x] Extend `DisplayMonitor.swift` to register a `CGDisplayRegisterReconfigurationCallback`. Debounce the documented dual-callback pattern (`beginConfigurationFlag` first, then the actual flags) so each logical reconfig fires the handler exactly once. Distinguish `primary_changed` (`.setMainFlag`), `resolution_changed` (`.setModeFlag`), `display_removed` (`.removeFlag`) for the event reason. **[Agent: macos-swift]**
  - [x] On any reconfig that touches the currently-captured primary display, `VideoCapture` tears down its current `SCStream` and recreates a new one against the (possibly new) primary display, with new pixel dimensions if needed. The `MP4Writer` is kept (the writer accepts buffers of changing dimension; resolution discontinuity is acceptable per functional spec §2.7). Emit `display_reconfigured` (per tech spec §2.6) with the reason and new dimensions. **[Agent: macos-swift]**
  - [x] Update `src/record/supervisor.py` to append each `display_reconfigured` event into `capture-state.json`'s `display_changes` array. **[Agent: python-backend]**
    - Already in place from Slice 1 plumbing: `_apply_event` handles `ipc.DisplayReconfiguredEvent` by appending `{timestamp, reason, new_display_id, new_width_px, new_height_px}` to `current["display_changes"]` (supervisor.py §`_apply_event`). Slice 3 only activates the Swift side that emits these events; no Python changes were required. Confirmed by the existing `test_ipc`/`test_state`/`test_cli` suite (74 tests pass).
  - [x] **Verification (manual — requires an external monitor):** Start with an external monitor configured as primary. `record start` → display content for 10 s → unplug the external monitor (primary now falls back to the laptop display) → display content for another 10 s → `record stop`. Confirm: no crash; `.mp4` is playable end-to-end in QuickTime; `daemon.log` shows a `display_reconfigured` event with reason `display_removed` (or `primary_changed`); `capture-state.json` (during capture) or the orchestrator log (after stop) shows the corresponding `display_changes` entry. **[Agent: macos-swift]**
    - Code-level verification only in this session — `swift build -c debug --package-path swift-capture` is clean, `uv run pytest tests/python` is green (74/74). The actual external-monitor unplug scenario was **not** exercised here because no external monitor is attached to this agent session; per the "Notes on agent assignments / dependencies" table below, this verification is the developer's responsibility on real hardware. The implementation path is: `DisplayReconfigurationMonitor` (CG callback, debounced on `beginConfigurationFlag`, filtered to reconfigs touching the captured display or with `.setMainFlag`) → `VideoCapture.reconfigure(to:reason:)` (stops old SCStream, builds new one against the new primary, keeps the same `MP4Writer`, emits `display_reconfigured` only after the new stream is up) → Python supervisor appends to `display_changes` in `capture-state.json`. Monitor is torn down at the top of `handleStop` so no late CG callback can race the writer's `finishWriting`.

## Slice 4 — System sleep / display sleep / screen lock end the capture cleanly

Goal: closing the lid, locking the screen, or letting the display sleep all end the capture as cleanly as `record stop`. Both files are finalized and saved. The summary (in `orchestrator.log`) names the reason. No auto-resume on wake.

- [x] **Slice 4: System-event-triggered shutdown**
  - [x] Implement `swift-capture/Sources/RecordCapture/SystemEventMonitor.swift`: subscribe to `NSWorkspace.willSleepNotification` (reason `"system_sleep"`), `NSWorkspace.screensDidSleepNotification` (reason `"display_sleep"`), both on `NSWorkspace.shared.notificationCenter`; and `DistributedNotificationCenter.default()` observing `"com.apple.screenIsLocked"` (reason `"screen_locked"`). On the first such event during an active capture, mark itself shutting-down (idempotent) and trigger the same internal stop path as the `stop` command. **[Agent: macos-swift]**
  - [x] Wire `main.swift`'s startup to instantiate `SystemEventMonitor` on `start` and dispose it on `stop`. Just before emitting the existing `stopped` event from a system-event-triggered shutdown, emit `capture_ended_by_system_event` (with the reason and `at_offset_seconds`) so the orchestrator can distinguish it from a user-initiated stop. Bound the wait for `finishWriting` to ~5 s, then exit 0. **[Agent: macos-swift]**
  - [x] Update `src/record/supervisor.py` to: (a) detect the `capture_ended_by_system_event` event and set `capture-state.json`'s `ended_by` field accordingly; (b) on the Swift binary's clean exit triggered by a system event (no SIGTERM from `record stop`), write a one-line summary to `orchestrator.log` containing both file paths, total duration, and the reason; (c) clean up PID + state files as for a normal stop; (d) exit 0. No auto-resume. **[Agent: python-backend]**
    - (a) was already in place from Slice 1 plumbing (`_apply_event` for `CaptureEndedBySystemEventEvent` → sets `current["ended_by"] = event.reason`). (b) added at `supervisor.py:492–524`: after the read loop and before the final state write, the discriminator `saw_stopped and not stop_requested.is_set() and ended_by in {"system_sleep","display_sleep","screen_locked"}` selects system-event shutdowns and appends a single human-readable line to `~/Library/Logs/record/orchestrator.log` of the form `[<utc-iso>] capture ended by system event reason=<reason> audio=<abs.wav> [video=<abs.mp4>] [duration_seconds=<float>]`. `video=` and `duration_seconds=` are omitted when their values are missing (no `=None` noise); write is wrapped in `try/except OSError` and degrades to a `orchestrator_summary_write_failed` structlog warning rather than raising. (c) PID + state file cleanup is unchanged from the user-`stop` path — `state.write_state(current_state)` with `final=True`, PID cleanup happens in the CLI layer. (d) Exit 0: `saw_stopped=True`, `stop_requested.is_set()=False`, `abnormal=False` → `return _EXIT_OK`. No respawn or auto-resume logic introduced.
  - [x] **Verification (manual — repeat on macOS 13/14/15 if available, per tech spec §3 risk):** Three sub-scenarios:
    1. `record start` → close laptop lid after ~10 s → wait 5 s → reopen lid. Confirm: capture is NOT auto-resumed; `orchestrator.log` has a summary line with reason `system_sleep`; both `.wav` and `.mp4` are present, playable end-to-end, and have aligned start times (durations may differ by a few hundred ms — the encoder may flush slightly differently than the WAV writer).
    2. `record start` → ⌃⌘Q after ~10 s. Confirm: summary line with reason `screen_locked`; both files present and playable. (This validates the undocumented `com.apple.screenIsLocked` notification on this macOS version — if it does NOT fire, capture continues until manual stop, which is the documented degraded behavior; record that fact in the slice notes.)
    3. `record start` → wait for display sleep (set "Turn display off" to 1 min in System Settings → Lock Screen before testing). Confirm: summary line with reason `display_sleep`; both files present and playable. **[Agent: macos-swift]**
    - Code-level verification only in this session — `swift build -c debug --package-path swift-capture` is clean (no new warnings); `uv run pytest tests/python -q` is green (83/83). The three lid-close / ⌃⌘Q / display-sleep scenarios were **not** exercised here because they require a real Mac with hardware-level signals the agent session cannot drive; per the "Notes on agent assignments / dependencies" table below, this verification is the developer's responsibility. The implementation path is: `SystemEventMonitor` (`AppKit` + `DistributedNotificationCenter`, `NSLock`-guarded first-event-wins flag, callback re-dispatched to `.main`) → wiring layer in `main.swift` sets `state.endedBy = reason` then calls the existing `handleStop()` so finalize takes the same single codepath as a user `record stop` → just before `stopped`, if `state.endedBy` is non-nil, `main.swift` emits `capture_ended_by_system_event(reason, at_offset_seconds)` with the offset rounded to 3 decimals → Python supervisor recognizes the resulting clean exit (saw_stopped=True, stop_requested=False, ended_by ∈ system reasons) and appends a one-line summary to `orchestrator.log`. The undocumented `com.apple.screenIsLocked` notification is treated as best-effort: registration is non-throwing and structurally cannot fail at the API surface, so if a future macOS stops delivering it the failure mode is benign — capture simply continues until `record stop`, documented inline in `SystemEventMonitor.swift`. `state.systemEventMonitor?.stop()` is invoked as the very first step of `handleStop` so a second notification arriving mid-finalize cannot re-enter the stop path.

## Slice 5 — Best-effort video: failures never take down audio

Goal: any video failure (Screen Recording denied, SCStream crash mid-capture, writer failure) leaves audio capture intact. The `.wav` is still produced. The `.mp4` is finalized with whatever was captured up to the failure, or omitted entirely if video never started.

- [x] **Slice 5: Video best-effort resilience**
  - [x] In `VideoCapture.swift`, install an `SCStreamDelegate` that handles `stream(_:didStopWithError:)`: tear down the writer via `MP4Writer.finalize()` (so a partial mp4 is saved and playable), emit `video_lost` with `at_offset_seconds` and reason (`sc_stream_error` for SCK errors, `writer_failure` for `AVAssetWriter` failures). Do NOT touch audio capture. **[Agent: macos-swift]**
  - [x] Handle Screen Recording permission denial cleanly: if `VideoCapture` cannot create its `SCStream` at start (e.g., `SCShareableContent.current` throws), emit `video_lost` with `at_offset_seconds = 0` and reason `permission_denied`, do NOT produce an `.mp4`, and let `AudioCapture` proceed as normal. **[Agent: macos-swift]**
  - [x] Update `src/record/supervisor.py` to handle `video_lost`: set `video.status = "lost"`, set `lost_at`, append to `warnings`. If `at_offset_seconds == 0` (never started), do NOT include a video file path in the summary. **[Agent: python-backend]**
  - [x] Update `src/record/cli.py` summary printer: when `video.status = "lost"`, render `"video: unavailable — <reason>"` (offset 0) or `"video: <path> — stopped at HH:MM:SS, reason <reason>"` (mid-capture). **[Agent: python-backend]**
  - [x] **Verification (manual):** Two sub-scenarios.
    1. Revoke Screen Recording in System Settings → Privacy & Security → Screen Recording → `record start`. Confirm: warning logged, audio sources still attach, mic + system audio sample for ~10 s, `record stop` → `.wav` produced, NO `.mp4` produced, summary says `"video: unavailable — permission_denied"`. Re-grant Screen Recording.
    2. Trigger a synthetic SCStream error mid-capture (simplest hack: use a debug build that takes a `--simulate-video-failure-after-seconds <N>` flag wired into `VideoCapture` to call its own `stop()` and emit `didStopWithError`). Run for ~20 s with simulated failure at 10 s. Confirm: summary shows audio captured for ~20 s, video stopped at ~10 s, mp4 is playable end-to-end with ~10 s duration. **[Agent: macos-swift]**
    - Swift-side code-level verification only in this session — `swift build -c debug --package-path swift-capture` is clean (no warnings). `swift test` could not be run because this environment has only Command Line Tools (no XCTest bundle); same constraint that applied to the slice 3 and slice 4 notes above. The actual manual scenarios are the developer's job on real hardware (the macOS TCC dialog cannot be driven by an agent in CI).
    - **Scenario 1 code path** (revoke Screen Recording → `record start`): `AudioCapture.checkPermissions()` runs first and (per `Permissions.checkScreenRecording`) emits `permission_denied(kind: screen_recording)`; audio sources still attach and the supervisor proceeds. Then `handleStart` enters the video block: `DisplayMonitor.resolvePrimary()` calls `SCShareableContent.excludingDesktopWindows(...)` which throws an `NSError` in `SCStreamErrorDomain` with code `-3801` (`SCStreamError.Code.userDeclined`). The error bubbles out of `try await DisplayMonitor.resolvePrimary()` directly (no `VideoCaptureError` wrap on that path), so `isPermissionDenied(error)` matches on the raw `NSError` domain+code, `classifyVideoStartupError` returns `"permission_denied"`, and `emit(.videoLost(atOffsetSeconds: 0, reason: "permission_denied", ...))` fires. The catch block then runs `FileManager.default.removeItem(at: videoURL)` because `writerForCleanup` is still nil (MP4Writer was never constructed) so `writerForCleanup?.hasStartedSession != true` is true → no zero-byte mp4 left on disk. Audio capture is untouched and continues to `record stop`, producing the `.wav` normally.
    - **Scenario 2 code path** (`--simulate-video-failure-after-seconds 10`, capture for ~20 s): `parseCLIFlags` records the value into `cliFlags.simulateVideoFailureAfterSeconds`. After `vc.start(display:)` succeeds, `handleStart` schedules `DispatchQueue.main.asyncAfter(deadline: .now() + 10) { capture?.videoCapture?.simulateStreamFailure() }`. At t≈10 s `VideoCapture.simulateStreamFailure()` synthesizes an `NSError(domain: "SCStreamErrorDomain", code: -3808, ...)` and calls `handleStreamError`. That path goes: `claimVideoLoss()` flips first-event-wins → emit `video_lost(reason: "sc_stream_error", atOffsetSeconds: ~10.0)` → `writer.finalizeSync(timeout: 2.0)` writes the moov atom now, leaving a playable ~10 s mp4. Audio is untouched — `AudioCapture` keeps running until the user `record stop` at ~20 s. At stop, `handleStop` calls `vc.stop()` which calls `writer.finalize()` again; that returns the cached duration from `prepareFinalize`'s `.alreadyDone(.finished)` branch and `video_file` is emitted with the partial duration in the existing wire order. Composes cleanly with `--test-silent-sources`: the two flags are independent parsing branches in `parseCLIFlags`.
    - **`writer_failure` code path** (not directly exercised by the two manual scenarios above, but implemented for completeness per the slice's invariant): if `AVAssetWriter` flips to `.failed` during `append` or `finishWriting`, `MP4Writer.fireAsyncFailureOnce` calls back into `VideoCapture.handleWriterFailure`, which claims the same `lossLock` and emits `video_lost(reason: "writer_failure", ...)`. The single-claim guarantee means an SCStream error and a writer failure can't both emit; whichever wins reports the cause.

## Slice 6 — Automated tests, `--test-synthetic-video` flag, and README

Goal: the slice verifications no longer rely solely on a real display + TCC. `make test` runs the extended unit + integration suite. Manual smoke test is documented.

- [x] **Slice 6: Tests & docs**
  - [x] Add a `--test-synthetic-video` flag to the Swift binary that bypasses `SCStream` entirely and feeds `MP4Writer` with deterministic synthetic `CMSampleBuffer`s (single-color `CVPixelBuffer`s of a fixed 640×360 size with monotonically increasing PTS at 30 fps). No TCC permissions required, no display hardware required, CI-friendly. Composes with the existing `--test-silent-sources` audio flag. **[Agent: macos-swift]**
  - [x] Add `swift-capture/Tests/RecordCaptureTests/MP4WriterTests.swift`: feed `MP4Writer` a known sequence of synthetic buffers, call `finalize()`, re-open the resulting `.mp4` with `AVAsset` and assert non-zero file size, exactly one video track, duration within ±100 ms, and dimensions/fps matching config. **[Agent: macos-swift]**
  - [x] Add `swift-capture/Tests/RecordCaptureTests/DisplayMonitorTests.swift`: assert `CGDisplayPixelsWide(CGMainDisplayID())` and `CGDisplayPixelsHigh(CGMainDisplayID())` are returned (sanity check + documents the pixels-vs-points convention). **[Agent: macos-swift]**
  - [x] Extend `swift-capture/Tests/RecordCaptureTests/ProtocolTests.swift` with round-trips for the new events (`video_started`, `video_lost`, `video_file`, `display_reconfigured`, `capture_ended_by_system_event`) and the extended `start` command against shared JSON fixtures with Python. **[Agent: macos-swift]**
  - [x] Extend `tests/python/test_ipc.py` with round-trips for the new event/command shapes. Extend `tests/python/test_state.py` to assert the widened schema accepts `video`, `display_changes`, `ended_by`. Extend `tests/python/test_cli.py` to verify the stop summary renders the video file path / video status correctly for `attached`, `lost (offset 0)`, `lost (mid-capture)`, and `never_attached` cases against a stub supervisor that emits scripted events. **[Agent: python-backend]**
  - [x] Extend `tests/integration/test_end_to_end.py` to also use `--test-synthetic-video` alongside `--test-silent-sources`. Assert event sequence (`ready` → `started` → `source_attached` ×N → `video_started` → `stopped` → `video_file`); assert resulting `.mp4` opens with `AVAsset` (via PyObjC or `ffprobe`-based fallback), has expected dimensions and ≈expected duration; assert `.wav` and `.mp4` share the same filename stem. **[Agent: python-backend]**
  - [x] Update the project `README.md` with: the new `.wav` + `.mp4` output pair; the Screen Recording permission first-run flow (already required by 001 — note that it now also gates video); the new manual smoke tests for system-event shutdown (slice 4) and best-effort video (slice 5); pointer to `daemon.log` / `orchestrator.log` for diagnosing video issues. **[Agent: general-purpose]**
  - [x] **Verification:** Run `make test` from a clean checkout — all unit and integration tests pass without requiring any TCC permissions or attached display. Manually re-run the slice 2 smoke test from the README and confirm both files are still produced correctly. **[Agent: python-backend]**
    - `make test` is green from a clean state: `make swift` rebuilt the Release `record-capture` binary into `src/record/bin/`; `uv run pytest tests` reports **127 passed in 4.35s** covering `tests/python` (test_cli: 22, test_ipc: 50, test_paths: 9, test_state: 44) and `tests/integration` (2 tests, including the new `test_end_to_end_silent_sources_with_synthetic_video` that drives both `--test-silent-sources` and `--test-synthetic-video` end-to-end and validates the resulting `.mp4` via a three-tier probe — pyobjc `AVAsset` → `ffprobe` → built-in MP4-atom parser, so the test runs zero-dep on any macOS dev machine). `swift test` gracefully skipped via the Makefile's own message because this environment has Command Line Tools only, not full Xcode — same constraint that applied to the slice 3 / 4 / 5 notes; the slice's promised happy-path coverage now lives in the python integration test rather than XCTest, which is fine for now and the Swift tests will run on any developer machine with full Xcode.
    - The slice-2 manual smoke test from the README (real `record start` → display content for 30 s → `record stop`, then visually inspect the `.mp4` in QuickTime, confirm cursor visible, dimensions match `CGDisplayPixelsWide/High`) was **not** performed in this agent session — it requires real display hardware, TCC-granted Screen Recording, and a developer at the machine. Same hardware-bound caveat the slice 2 / 3 / 4 / 5 notes already document for their own manual verifications. The "Notes on agent assignments / dependencies" table below covers this class of verification explicitly.
    - Swift-side additions for the slice: `swift-capture/Sources/RecordCapture/SyntheticVideoSource.swift` (new `VideoSource` protocol + 640×360 30 fps deterministic generator, composes with `--test-silent-sources` and `--simulate-video-failure-after-seconds`), `MP4WriterTests.swift`, `DisplayMonitorTests.swift` (skips on headless / no-TCC), and 6 new JSON fixtures under `Tests/RecordCaptureTests/Fixtures/{commands,events}/` shared with the Python `test_ipc.py` round-trips. `swift build -c debug --package-path swift-capture` is clean.

---

### Notes on agent assignments / dependencies

| Item | Issue | Recommendation |
|---|---|---|
| Slice 6 sub-task 7 (README update) | Assigned to `general-purpose` — no dedicated docs specialist | Acceptable; small doc edit. No new agent needed. |
| Slice 2 / 3 / 4 / 5 verifications | Require macOS TCC (Screen Recording) granted on a real Mac, plus an attached external monitor (Slice 3) and the ability to close the lid / lock the screen (Slice 4) | Cannot be performed by an agent in CI — must be run on the developer's machine. Slice 6's `--test-synthetic-video` integration test gives an automated approximation for the happy path. |
| Slice 4 macOS-version matrix | `com.apple.screenIsLocked` reliability across macOS 13/14/15 is the spec's biggest open empirical question (tech spec §3 risk) | Repeat the screen-lock sub-test on each macOS version the developer has access to. If the notification does not fire on a given version, document the degradation in the slice's notes and `daemon.log` — capture will simply continue until manual stop. |
| Slice 5 sub-task 2 (synthetic video failure) | Requires a `--simulate-video-failure-after-seconds` debug flag in the Swift binary; not yet a designed feature | Add it as part of slice 5 (debug-only, gated behind an `#if DEBUG` or always-on but undocumented). Alternative: use Slice 6's `--test-synthetic-video` integration test to script the same failure injection. |
